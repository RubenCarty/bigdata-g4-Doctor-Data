services:
  frontend:
    build:
      context: .
      target: local
    container_name: doctordata-frontend
    ports:
      - "3000:3000"
    env_file: .env
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
